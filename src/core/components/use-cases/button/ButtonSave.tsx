import { FunctionComponent } from 'react';
import { AppButton, AppButtonProps } from '@core/components';

export const AppButtonSave: FunctionComponent<AppButtonProps> = (
  props: AppButtonProps,
) => <AppButton {...props} />;
